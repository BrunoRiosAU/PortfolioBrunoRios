
import styles from "../app/projects/[id]/page.module.css"

export default function PageNotFound() {
    return (
        <div className={styles.pageNotFound}>
            <h1 className="primaryText">Page not found</h1>
        </div>
    )
}